pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
    }
}

rootProject.name = "CloudStream"
include(":app")
include(":library")
include(":docs")
